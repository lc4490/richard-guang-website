/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "img-de-v2.gtsstatic.net",
      "mmsmedia.vht.com",
      "static-ind-elliman-newyorkcity-production.gtsstatic.net",
    ], // Add the domains where your images are hosted
  },
};

export default nextConfig;
